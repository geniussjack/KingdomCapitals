# 🚨 КРИТИЧЕСКИ ВАЖНО: Инструкция по обновлению мода

## Проблема
Игра загружает **СТАРУЮ версию DLL файла**. Новые патчи не применяются (Total methods patched: 0).

## Решение: Полная переустановка мода

### Шаг 1: Очистка старых файлов

1. **Закройте игру полностью**
2. **Удалите папку мода**:
   ```
   Mount & Blade II Bannerlord\Modules\KingdomCapitals\
   ```
3. **Очистите shader cache**:
   ```
   C:\ProgramData\Mount and Blade II Bannerlord\Shaders\
   ```
   Удалите папки: `TerrainShaders` и `CoreShaders`

### Шаг 2: Пересборка проекта

1. Откройте проект в **Visual Studio** или **Rider**
2. Выполните **Clean Solution**
3. Выполните **Rebuild Solution**
4. Убедитесь, что сборка прошла БЕЗ ОШИБОК

### Шаг 3: Разблокировка DLL файлов (ОБЯЗАТЕЛЬНО!)

Windows блокирует DLL файлы, скачанные из интернета!

**Вариант A (PowerShell - быстро):**
```powershell
cd "путь\к\KingdomCapitals\bin\Win64_Shipping_Client"
dir | Unblock-File
```

**Вариант B (вручную):**
1. Перейдите в `KingdomCapitals\bin\Win64_Shipping_Client\`
2. Для КАЖДОГО .dll файла:
   - Правой кнопкой → Свойства
   - Внизу галочка "Разблокировать" (Unblock)
   - OK

### Шаг 4: Установка мода

1. Скопируйте папку `KingdomCapitals` в:
   ```
   Mount & Blade II Bannerlord\Modules\
   ```
2. Убедитесь, что структура правильная:
   ```
   Modules\
     KingdomCapitals\
       bin\
         Win64_Shipping_Client\
           KingdomCapitals.dll  <-- ЭТОТ ФАЙЛ ДОЛЖЕН БЫТЬ НОВЫМ!
       SubModule.xml
       ...
   ```

### Шаг 5: Проверка порядка загрузки модов

В лаунчере игры убедитесь, что порядок такой:
1. Bannerlord.Harmony (ПЕРВЫМ!)
2. ButterLib
3. MCM (Bannerlord.MBOptionScreen)
4. KingdomCapitals
5. Остальные моды

### Шаг 6: Проверка логов

После запуска игры проверьте логи. Должно быть:

```
[INFO] CapitalGarrisonWagePatch: ENABLED - Capital garrisons will be free
[INFO] DefaultPartyWageModel_GetTotalWage_Patch: ENABLED
[INFO] Building_IsCurrentlyDefault_Patch: ENABLED
[INFO] Building_GetBuildingEffectAmount_Patch: ENABLED
[INFO] Total methods patched: 4   <-- ДОЛЖНО БЫТЬ > 0 !!!
```

## Проблема с исчезновением названий поселений

Это **НЕ вина моего мода** если "Total methods patched: 0". Мои патчи просто не загрузились.

Возможные причины:
1. **Другой мод конфликтует** - попробуйте отключить все моды кроме KingdomCapitals
2. **Повреждённый shader cache** - удалите как указано выше
3. **Баг самой игры** - в ванильной игре есть баг с исчезновением названий при зуме

### Тест: Запустите игру БЕЗ модов
Если названия исчезают и без модов - это баг игры, не мода.

## Проверка FoodChange

После обновления мода, гарнизон должен расти ТОЛЬКО когда:
1. В меню поселения под "Food" показывается зелёная надпись "+X"
2. FoodChange > 0

Если всё ещё растёт при уменьшении еды - значит DLL всё ещё старый!

---

**Если после ВСЕХ этих шагов проблемы остаются**, пришлите:
1. Полные логи (весь файл)
2. Список всех установленных модов
3. Версию игры
