# Инструкция: Файлы для реализации UI-патчей (золотой фон столиц)

## 📋 Необходимые файлы игры для анализа

Для правильной реализации визуального выделения столиц золотым фоном вам потребуется проанализировать следующие **DLL-библиотеки** из вашей установки Bannerlord v1.2.12:

---

## 1. 🎯 Основные DLL для декомпиляции

### **Путь к файлам:**
```
D:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\
```

### **Файлы для анализа:**

#### A. **TaleWorlds.CampaignSystem.ViewModelCollection.dll**
- **Местоположение:** `\Modules\SandBox\bin\Win64_Shipping_Client\`
- **Назначение:** Содержит все ViewModel классы для UI кампании
- **Что искать:**
  - `MapBarVM` - ViewModel для верхней панели карты
  - `MapBarSettlementItem` - элемент поселения в MapBar (если существует)
  - `SettlementNameplateVM` - ViewModel для таблички с названием поселения
  - `MapInfoVM` - информация на карте
  - Любые классы с "Settlement" и "Tooltip" в названии

#### B. **SandBox.View.dll**
- **Местоположение:** `\Modules\SandBox\bin\Win64_Shipping_Client\`
- **Назначение:** View-слой для отображения UI кампании
- **Что искать:**
  - `MapScreen` - основной экран карты кампании
  - Классы, связанные с отображением tooltip при наведении на поселения
  - View-классы для settlement nameplate

#### C. **TaleWorlds.MountAndBlade.ViewModelCollection.dll**
- **Местоположение:** `\bin\Win64_Shipping_Client\`
- **Назначение:** Базовые ViewModel компоненты
- **Что искать:**
  - Базовые классы для tooltip
  - Общие UI компоненты

---

## 2. 🔧 Инструменты для декомпиляции

### **Рекомендуемые декомпиляторы:**

#### **ILSpy** (рекомендуется)
- **Скачать:** https://github.com/icsharpcode/ILSpy/releases
- **Версия:** ILSpy 8.x или новее
- **Преимущества:**
  - Бесплатный, open-source
  - Отличная поддержка .NET Framework 4.7.2
  - Экспорт в C# проект
  - Поиск по типам и методам

#### **dnSpy** (альтернатива)
- **Скачать:** https://github.com/dnSpy/dnSpy/releases
- **Версия:** dnSpy 6.x
- **Преимущества:**
  - Отладчик встроен
  - Редактирование DLL на лету
  - Удобный поиск

---

## 3. 🔍 Пошаговая инструкция по анализу

### **Шаг 1: Открыть DLL в ILSpy**

1. Запустите ILSpy
2. File → Open → выберите `TaleWorlds.CampaignSystem.ViewModelCollection.dll`
3. Дождитесь загрузки

### **Шаг 2: Поиск классов для tooltip поселений**

В ILSpy выполните поиск (Ctrl+Shift+F):

```csharp
// Поисковые запросы:
1. "Settlement.*Tooltip"
2. "Settlement.*Nameplate"
3. "MapBar.*Settlement"
4. "RefreshValues"
5. "UpdateProperties"
```

### **Шаг 3: Анализ найденных классов**

Для каждого найденного класса проверьте:

#### **A. Структура ViewModel:**
```csharp
public class SettlementSomethingVM : ViewModel
{
    // Ищите свойства:
    private Settlement _settlement;
    public Settlement Settlement { get; }

    // Ищите методы:
    public override void RefreshValues()
    public void UpdateProperties()
    public void SetSettlement(Settlement settlement)
}
```

#### **B. Свойства для модификации:**
```csharp
// Ищите свойства цвета/фона:
public Color BackgroundColor { get; set; }
public Color NameColor { get; set; }
public string ColorHex { get; set; }
public uint ColorValue { get; set; }
```

#### **C. Методы для патчинга:**
```csharp
// Целевые методы для Harmony патчей:
- RefreshValues()
- UpdateProperties()
- OnHoverBegin()
- SetTooltipProperties()
```

---

## 4. 📝 Что конкретно нужно найти

### **Цель 1: Класс для tooltip при наведении на поселение**

**Где искать:**
- `TaleWorlds.CampaignSystem.ViewModelCollection.Map.*`
- Классы с "Nameplate", "Tooltip", "Hover"

**Пример того, что вы можете найти:**
```csharp
namespace TaleWorlds.CampaignSystem.ViewModelCollection.Map
{
    public class SettlementNameplateVM : ViewModel
    {
        private Settlement _settlement;
        private Color _backgroundColor;

        public void RefreshValues()
        {
            // Этот метод обновляет UI
            // Здесь нужно создать Harmony патч
        }
    }
}
```

### **Цель 2: Свойство цвета фона**

**Что искать:**
```csharp
// Возможные имена свойств:
- BackgroundColor
- NameplateColor
- TooltipBackgroundColor
- BorderColor
- HighlightColor
```

**Формат цвета:**
```csharp
// Может быть одним из:
1. Color (TaleWorlds.Library.Color)
2. uint (ARGB формат: 0xFFCBAE79)
3. string (HEX: "#cbae79")
```

---

## 5. 🎨 Примеры патчей на основе найденной информации

После того как вы найдете нужные классы, создайте патч:

### **Пример 1: Если найден SettlementNameplateVM**

```csharp
[HarmonyPatch(typeof(SettlementNameplateVM), "RefreshValues")]
public static class SettlementNameplate_RefreshValues_Patch
{
    static void Postfix(SettlementNameplateVM __instance)
    {
        // Используем рефлексию для доступа к Settlement
        var settlementField = typeof(SettlementNameplateVM)
            .GetField("_settlement", BindingFlags.NonPublic | BindingFlags.Instance);

        if (settlementField != null)
        {
            Settlement settlement = settlementField.GetValue(__instance) as Settlement;

            if (settlement != null && CapitalManager.IsCapital(settlement))
            {
                // Применяем золотой цвет
                var colorProperty = typeof(SettlementNameplateVM)
                    .GetProperty("BackgroundColor");

                if (colorProperty != null)
                {
                    Color goldColor = Color.FromUint(0xFFCBAE79);
                    colorProperty.SetValue(__instance, goldColor);
                }
            }
        }
    }
}
```

### **Пример 2: Если используется строковое свойство**

```csharp
[HarmonyPatch(typeof(SomeTooltipVM), "UpdateProperties")]
public static class TooltipColor_Patch
{
    static void Postfix(SomeTooltipVM __instance)
    {
        if (__instance.Settlement != null && CapitalManager.IsCapital(__instance.Settlement))
        {
            // Если используется HEX-строка
            typeof(SomeTooltipVM)
                .GetProperty("BackgroundColorHex")
                ?.SetValue(__instance, "#cbae79");
        }
    }
}
```

---

## 6. 📊 Информация для меня

### **Что мне прислать после анализа:**

#### **Формат отчета:**

```markdown
## Найденные классы:

### Класс 1: [Полное имя класса с namespace]
- **Файл:** TaleWorlds.CampaignSystem.ViewModelCollection.dll
- **Namespace:** TaleWorlds.CampaignSystem.ViewModelCollection.Map
- **Свойства:**
  - Settlement _settlement (private field)
  - Color BackgroundColor { get; set; }
- **Методы:**
  - RefreshValues()
  - UpdateTooltip()

### Класс 2: ...
```

#### **Скриншоты из ILSpy:**
1. Дерево классов с выделенным нужным классом
2. Код класса с свойствами
3. Код метода, который будем патчить

#### **Код класса:**
Экспортируйте полный код найденного класса из ILSpy (правой кнопкой → Export to Project)

---

## 7. 🚀 Альтернативные подходы (если не найдете нужные классы)

### **План Б: Модификация названий напрямую**

Если UI классы недоступны, можно:

1. **Изменить сами названия поселений:**
   ```csharp
   // Добавить префикс/суффикс к названию
   settlement.Name = new TextObject("{=capital}[CAPITAL] " + settlement.Name);
   ```

2. **Использовать Localization:**
   - Модифицировать `std_module_strings_xml.xml`
   - Добавить цветовые теги в локализацию

3. **Custom ViewModel (сложно):**
   - Создать собственный ViewModel
   - Зарегистрировать через UIResourceManager

---

## 8. ⚠️ Важные замечания

### **Совместимость:**
- Убедитесь, что анализируете **точно v1.2.12**, а не другую версию
- Проверьте версию DLL в свойствах файла (ПКМ → Свойства → Подробно)

### **Безопасность:**
- НЕ модифицируйте оригинальные DLL
- Работайте только с **копиями** файлов

### **Тестирование:**
- После создания патча тестируйте на чистом сохранении
- Проверьте производительность (патчи на RefreshValues вызываются часто)

---

## 9. 📞 Что делать дальше

1. **Декомпилируйте** указанные DLL с помощью ILSpy
2. **Найдите** классы для tooltip поселений
3. **Пришлите мне:**
   - Название класса
   - Namespace
   - Список свойств (особенно связанных с цветом)
   - Код метода RefreshValues или аналогичного
4. **Я создам** корректный Harmony-патч на основе вашей информации

---

**Готово!** После получения информации о реальных классах из v1.2.12 я смогу реализовать рабочие UI-патчи для золотого фона столиц.
